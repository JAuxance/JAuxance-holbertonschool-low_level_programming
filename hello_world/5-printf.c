#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print a specific phrase to the standard output
 *
 * Return: Always 0 (Sucesse)
 */
int main(void)
{
char mytext[] = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", mytext);

return (0);
}
