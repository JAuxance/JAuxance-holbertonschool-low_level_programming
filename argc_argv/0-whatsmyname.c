#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * main - prints its name
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	{
		puts(argv[0]);
	}
	return (0);
}
